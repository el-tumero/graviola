---
import { Icon } from "astro-icon/components"
import NavElementWrapper from "./NavElementWrapper.astro"

interface Props {
    icon: string
    url?: string
    additionalClasses?: string
}

const { icon, url } = Astro.props
---

<NavElementWrapper url={url}>
    <Icon size="24" name={icon} />
</NavElementWrapper>
