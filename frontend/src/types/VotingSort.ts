
export enum SortingType {
    BY_ID = 1,
    BY_SCORE_ASC,
    BY_SCORE_DESC,
    BY_KEYWORD_ASC,
    BY_KEYWORD_DESC,
    // BY_KWORD_ITER_ASC, // Future default?
    // BY_KWORD_ITER_DESC,
    // BY_BADGE           // Cache
    // BY_TRENDING
}